{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
    <h1>Create new User</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Dodaj') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_index')}}" class="btn btn-primary" role="button">Admin Index</a>

{% endblock %}
